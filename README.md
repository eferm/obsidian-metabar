# Note Dates Bar

Displays file creation and modification dates in a small bar above the editor.

## Features

- Shows created and modified timestamps at the top of each note in edit mode
- Uses your system locale for date formatting

